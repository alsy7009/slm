# CircuitSight — Evaluation Results

*Last updated 2026-07-11. All numbers are from actual runs (captured from the Colab run outputs); see "How to reproduce". Two models are reported: **v1** (`…20260709_1`) and **v2** (`…20260710_1`, PLAN-retrained for tool-offload) — see "Update — Model v2".*

## TL;DR

Fine-tuning a 3B vision-language model on our circuit dataset produces **large gains in "reading"
the schematic** — identifying components (0.17 → 0.96), naming the reactive type (0.33 → 1.00),
locating parts with bounding boxes (grounding 0.00 → 0.84), and **answering honestly** on unreadable
values (0.00 → 1.00) — while **solving for the final number stays low** (answer accuracy 0.00 → 0.09).

Splitting the answer accuracy by how values are written shows *why*: the model scores **0%** when it
must do the **arithmetic** (numeric problems) but **~31%** when the answer is an **algebraic
expression** (symbolic problems — same reading and reasoning, no arithmetic). So the weakness is
*arithmetic*, not *circuit reasoning*. That is exactly the moat we set out to build (perception +
correct setup), with the arithmetic offloadable to a calculator.

---

## Model evaluated (v1)

| | |
|---|---|
| **Artifact** | `circuitsight_qlora_final_20260709_1.zip` (Google Drive `MyDrive/slm_project/models/`) |
| **Base model** | `Qwen2.5-VL-3B-Instruct`, loaded in 4-bit |
| **Fine-tuning** | QLoRA (LoRA rank 16, α 16) on both vision and language layers |
| **Training** | `MAX_STEPS = 350`, effective batch 8 (~2,800 images seen), images ≤ 512 px, lr 2e-4, final training loss ≈ 0.16, on an A100-40GB |
| **Output format** | For a schematic + question: every component with a bounding box `<box>[x0,y0,x1,y1]</box>` (0–1000 coords), the topology (series/parallel), a step-by-step solution, and a final line `FINAL: {"quantity","target_id","value","unit","abstain"}` |

Run A (base vs. tuned) was measured right after training; Run B (value-mode split) was measured by
reloading the saved `.zip` — it reproduced the in-session numbers exactly, confirming the saved
artifact is the trained model.

---

## What we tested (so a reader who hasn't seen the data can understand it)

**Testing set = a held-out *synthetic* validation split** (`val_synthetic.jsonl`, **500 records**;
the training set is 9,500). The circuits are generated by the same procedural pipeline as training —
a numerical circuit solver produces the ground-truth labels — but this 5% split is **never seen during
training**. Each record is one **schematic image + one question**, paired with exact gold labels: the
component list, a bounding box per component, the topology, the correct final answer, and the
intermediate values (equivalent resistance, per-branch currents).

The set spans:

- **Circuit families:** DC-resistor networks (series/parallel, ~60%), reactive circuits (a capacitor or
  inductor at steady-state or t=0, ~25%), and **Wheatstone bridges** (non-series-parallel, solved by
  nodal analysis, ~15% — the hard case a generic model can't reduce).
- **Value modes** (how component values appear on the diagram):
  - **numeric** — real numbers (`220Ω`, `12V`); solving needs actual arithmetic.
  - **symbolic** — letters (`R1`, `R2`, `V`); the correct answer is an algebraic expression
    (`R_eq = R1 + R2`). Tests reading + reasoning **with no arithmetic**.
  - **mixed** — some numbers and some symbols in one circuit.
  - Training composition (9,500 records): numeric 5,271 / symbolic 2,750 / mixed 1,479.
- **Question types:** current, voltage, power, equivalent resistance, charge, energy.
- **Abstention cases (~8%):** the value in the image is deliberately illegible; the correct behavior is
  to answer `null` ("I can't read it") instead of fabricating a number.

**Decoding:** greedy / deterministic (`temperature=0`); `max_new_tokens` = 256 for Run A, 400 for Run B.

**Sample sizes** (each run scores the first *N* rows of the val split):

| Run | Model(s) | N rows | Composition |
|---|---|---|---|
| **A — base vs. fine-tuned** | base + tuned | 24 | 23 solvable + 1 abstention; 6 reactive |
| **B — value-mode split** | tuned only | 36 | 33 solvable (14 numeric, 13 symbolic, 6 mixed) + 3 abstention |

These are small samples (fast iteration on limited GPU time). Treat single values as indicative — e.g.
Run A's abstention metrics rest on **1** case and reactive-type on **6**. The **numeric-vs-symbolic gap
in Run B** is the robust headline: it holds across 14 vs 13 samples and is large.

---

## Metric glossary (plain language)

**Reading / perception**
- **component_accuracy** — right *number* of resistors and correct detection of a capacitor/inductor.
- **reactive_type_accuracy** — on reactive circuits, named the *correct* one (not capacitor↔inductor).
- **grounding_recall** — of the real components, fraction boxed on-target (box overlap ≥ 0.5).
- **grounding_precision** — of the boxes it drew, fraction that landed on a real component.
- **grounding_f1 / grounding_hallucination_rate** — combined P/R; fraction of boxes hitting nothing.

**Solving**
- **Req_accuracy** — equivalent resistance (a key intermediate) correct.
- **intermediates_accuracy** — *every* branch current correct.
- **answer_accuracy** — final answer correct (numeric within 3%, or symbolically equivalent).
- **step_verified_accuracy** — strict: components **and** R_eq **and** all intermediates **and** final
  answer all correct (a right answer via wrong steps does not count).
- **answer_accuracy_by_value_mode / _by_qtype** — answer accuracy split by numeric/symbolic/mixed and
  by question type.

**Honesty (abstention cases)**
- **honest_abstention_rate** — correctly said `null` on an illegible value (higher better).
- **fabrication_rate** — invented a number instead (lower better).
- **abstention_precision / recall** — when it abstained, was it truly illegible / of illegible cases,
  how often it abstained.

**Concepts**
- **concept_declared_rate / concept_hallucination_rate** — stated the concepts used / cited one the
  problem doesn't need (lower better).

---

## Results

### Run A — Base vs. fine-tuned (n = 24 val records)

Fine-tuning turns "reading the circuit" from near-total failure into near-perfect, while solving for
the number stays low.

| Metric | Base | Fine-tuned | Δ |
|---|---|---|---|
| **Perception** | | | |
| component_accuracy | 0.167 | **0.958** | +0.792 |
| reactive_type_accuracy | 0.333 | **1.000** | +0.667 |
| grounding_recall | 0.000 | **0.842** | +0.842 |
| grounding_precision | n/a* | 0.848 | — |
| grounding_f1 | n/a* | 0.845 | — |
| grounding_hallucination_rate | n/a* | 0.153 | — |
| **Solving** | | | |
| Req_accuracy | 0.000 | 0.182 | +0.182 |
| intermediates_accuracy | 0.000 | 0.182 | +0.182 |
| answer_accuracy | 0.000 | 0.087 | +0.087 |
| step_verified_accuracy | 0.000 | 0.087 | +0.087 |
| **Honesty** (1 abstention case) | | | |
| honest_abstention_rate | 0.000 | **1.000** | +1.000 |
| fabrication_rate | 0.000 | 0.000 | 0.000 |
| abstention_recall | 0.000 | 1.000 | +1.000 |
| abstention_precision | n/a* | 1.000 | — |
| **Concepts** | | | |
| concept_declared_rate | 0.000 | 0.833 | +0.833 |
| concept_hallucination_rate | n/a* | 0.050 | — |

\* The base model produced no bounding boxes, never abstained, and declared no concepts, so these
precision-type metrics are undefined for it.

Answer accuracy by question type (fine-tuned): resistance 0.00, current 0.13, voltage 0.20, power 0.00,
charge 0.00, energy 0.00. (Base: 0.00 on all.)

### Run B — Fine-tuned answer accuracy by value mode (n = 36; 33 solvable)

The diagnostic that isolates the failure: same circuit families and reading task; the only difference
is whether the final answer requires arithmetic.

| Value mode | Answer accuracy | n | What it requires |
|---|---|---|---|
| **numeric** | **0.000** | 14 | read + reason + **arithmetic** |
| **symbolic** | **0.308** | 13 | read + reason, **no arithmetic** |
| **mixed** | 0.333 | 6 | mostly reasoning |
| overall | 0.182 | 33 | |

---

## Key finding

The fine-tuned model **reads and sets up circuits well but cannot reliably do the arithmetic**:

- Perception jumps from near-zero to near-perfect: components 0.17 → 0.96, reactive type 0.33 → 1.00,
  grounding 0.00 → 0.84, and it learns to say "I can't read it" (honest abstention 0.00 → 1.00) instead
  of guessing.
- On **symbolic** problems — answer is an expression like `R_eq = R1 + R2`, requiring correct reading
  and reasoning but **no arithmetic** — it is right **~31%** of the time.
- On **numeric** problems — identical circuits requiring multi-step arithmetic — it is right **0%** of
  the time.

A model that failed at *reasoning* would score near 0 on symbolic too. Instead the gap is entirely on
the *arithmetic* step. This is the intended result: **the moat is perception + correct setup** (which
generic models fail at, as the base column shows), and **the arithmetic is offloadable to a
calculator/solver** — feeding the model's setup into a solver would lift numeric accuracy toward the
symbolic level.

**Caveats:** samples are small (abstention rests on 1 case in Run A); symbolic accuracy is 31%, not
near-perfect, so the claim is *relative* (reasoning ≫ arithmetic), not "reasoning is solved."

---

## Update (2026-07-11) — Model v2: PLAN-retrained (`circuitsight_qlora_final_20260710_1`)

After making tool-offload a **trained** capability — dataset regenerated with `INCLUDE_PLAN=True` so each legible resistor target ends with a `PLAN` line (the answer as a formula in the component labels + the values read) — we retrained and re-ran the base-vs-tuned eval on **n=60** held-out records (5 abstention, 11 reactive).

**Reliable base→tuned gains (perception, intermediates, concepts, honesty):**

| Metric | Base | Tuned (v2) | Δ |
|---|---|---|---|
| component_accuracy | 0.233 | 0.850 | +0.617 |
| reactive_type_accuracy | 0.545 | 1.000 | +0.455 |
| grounding_recall | 0.000 | 0.799 | +0.799 |
| Req_accuracy | 0.000 | 0.537 | +0.537 |
| intermediates_accuracy | 0.000 | 0.250 | +0.250 |
| concept_declared_rate | 0.017 | 0.917 | +0.900 |
| concept_hallucination_rate | 0.000 | 0.036 | +0.036 † |
| honest_abstention_rate | 0.200 | 0.800 | +0.600 |
| abstention_recall | 0.200 | 0.800 | +0.600 |
| abstention_precision | 0.250 | 0.400 | +0.150 |

† base ≈ 0 only because the base model rarely declares any concept (declared_rate 0.017), so it can't "hallucinate" one; the tuned model declares concepts on 92% of items and cites an unneeded one only 3.6% of the time.

**v2 vs v1 (directional — different sample sizes and a different regenerated val split, so not apples-to-apples):**
- **Better in v2:** equivalent-resistance `Req_accuracy` **0.18 → 0.54** (the biggest change — v2 gets the key intermediate right far more often), `intermediates` 0.18 → 0.25, `concept_declared` 0.83 → 0.92, `concept_hallucination` 0.05 → 0.04 (lower is better).
- **Slightly lower in v2:** `component_accuracy` 0.96 → 0.85, `grounding` 0.84 → 0.80 — most likely the larger/harder regenerated val set (v2's *base* is also different) plus a small dilution from the added PLAN line; `reactive_type` stayed perfect (1.00).
- **Abstention:** v1's figures rested on a single abstain case; v2's 5 cases are more reliable. v2 catches illegible values well (recall 0.80) but **over-abstains** somewhat (precision 0.40) — a calibration item to watch.

**Raw answer accuracy for v2 is intentionally omitted here — it was mis-measured.** The new `PLAN:` line (printed after `FINAL:`) broke the eval's *greedy* `FINAL` parser: it swallowed the PLAN line, the JSON parse failed, and every dc-resistor answer scored 0 (the misleading `answer_accuracy 0.087→0.018`, `symbolic 0.308→0.000`, `mixed 0.333→0.000` in that run). This is a **parser bug, not a model regression** — now fixed (non-greedy `FINAL` match). Re-run §9 and §9d on the v2 model for true numbers; **§9d tool-offload** (model emits the formula, sympy computes the arithmetic) is the intended payoff and is still pending.

---

## LLM-as-judge (holistic rubric) — v2 model, n=12

A **vision-capable Claude** (via the TrueFoundry gateway) scored base vs. tuned outputs **0–2** on the brief's Appendix A rubric, judging each output independently (blind), on 12 held-out synthetic val items. This complements the mechanical eval: it measures axes code can't — spec-adherence *in spirit*, robustness, teachability, and internal self-consistency. (`consistency` = internal self-consistency: does `FINAL` match the steps, does the verification confirm the answer, do declared concepts match usage.)

| Dimension | Base | Tuned | Δ |
|---|---|---|---|
| spec_adherence | 0.00 | 0.92 | +0.92 |
| robustness | 0.00 | 0.92 | +0.92 |
| task_quality | 0.08 | 1.08 | +1.00 |
| consistency | 0.00 | 1.08 | +1.08 |

*(0 = violates/useless, 1 = partially embodies, 2 = fully embodies. Judge: `claude-sonnet-5` via gateway; deterministic-ish, temperature default.)*

**Read:** fine-tuning lifts *every* holistic axis from ~0 to ~1. The base model produces no structured, grounded solution (near-0 across the board); the tuned model reliably follows perception → concepts → topology → solve → verify → FINAL. It lands at ~1 rather than ~2 because it embodies the behavior consistently but not flawlessly — the residual gaps below.

### Error analysis (from the judge's per-item notes)

Across the 12 items the tuned model reliably produces the required structure (grounded perception → concepts → topology → solve → verification → FINAL), which is why spec-adherence and robustness rise from 0 to ~0.9. The judge capped most items at **1 rather than 2** for four recurring reasons:

1. **Superficial verification (most common).** The self-check step is usually generic prose ("the answer is consistent") rather than a real numeric back-substitution, so it doesn't actually confirm the result (flagged on ~5 items, e.g. img_009360, img_007828, img_006873).
2. **Self-inconsistency on hard circuits.** On Wheatstone-bridge / nodal problems the stated node voltages don't satisfy the model's own KCL equations, so the final current contradicts its derivation (img_002430) — the lowest `consistency` scores cluster here.
3. **Perception slips on out-of-distribution inputs.** Misread values (150Ω→100Ω, a missing 10Ω in img_004283) and a **Zener diode mislabeled as a 12 V source** (img_006014) — component types absent from the training distribution.
4. **Over-symbolic answers + a format artifact.** It sometimes returns a symbolic-only answer when the values are legible without saying why (img_005791), and the trailing tool-offload **`PLAN` line is often malformed or placed after `FINAL`**, which the judge counts as a format break (img_009360, img_006873).

The worst case (img_003744, a power problem with no legible source value) failed all four dimensions: inconsistent symbolic math plus an order violation. **Net:** the fine-tune succeeds at instilling the *behavior and format* (the moat); the residual errors concentrate in (a) genuine verification, (b) hard multi-node reasoning, and (c) perception on unfamiliar components — all data/curriculum-addressable. A quick win, now that tool-offload is dropped, is to regenerate without the `PLAN` line (`INCLUDE_PLAN=False`) so it stops costing spec-adherence points.

---

## How to reproduce

1. Colab, GPU runtime (a free **T4** is enough — inference only). Run §1 install → restart → imports →
   §2 Config → §3 Load dataset (upload `circuitsight_dataset.zip`) → §9 harness.
2. **Run B (value-mode split):** run **§9c**, setting
   `MODEL_ZIP = ".../slm_project/models/circuitsight_qlora_final_20260709_1.zip"` and `EVAL_N_SPLIT`
   (36 here; raise to 60–80 for firmer per-mode counts).
3. **Run A (full base-vs-tuned table):** run the §9 base-vs-tuned cell.

**Not yet run:** the **real-world transfer eval** (§10) on the 8 hand-labeled real AP-exam / open-source
diagrams — the intended *headline* number, to be reported once measured (synthetic accuracy overstates
real performance, so expect it to be lower).
